#include <stdio.h>

int main(){
    int N, vetor[101];

    for(int i=0; i < 101; i++){
        scanf("%d", &N);
        
        vetor[i] = N;
    }
    
    for(int i=0; i<100; i++){
    	
        if(vetor[100] == vetor[i]){
            printf("%d\n", i);
        }
    }
    
    return 0;
}
