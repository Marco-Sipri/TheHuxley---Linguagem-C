#include <stdio.h>

int num = 0;

void CONTA(){
	num++;
};

int main(){
	int x, n, aux = 0;
	
	scanf("%d", &x);
	scanf("%d", &n);
	int vetor[n];
	
	for(int i = 0; i < n; i++){
		scanf("%d", &vetor[i]);
		if(vetor[i] == x){
			CONTA();
		}
	}
	
	printf("%d\n", num);
	
	return 0;
}
